# ABOUTME: PipeWire drop-in config for non-systemd systems (Artix)
# ABOUTME: Starts wireplumber and pipewire-pulse via context.exec
context.exec = [
    { path = "/usr/bin/wireplumber" args = "" }
    { path = "/usr/bin/pipewire" args = "-c pipewire-pulse.conf" }
]
