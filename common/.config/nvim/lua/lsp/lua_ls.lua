-- ABOUTME: Lua LSP configuration for Neovim development
-- ABOUTME: Configures lua_ls to recognize vim global and Neovim runtime

return {
    settings = {
        Lua = {
            runtime = {
                version = "LuaJIT",
            },
            diagnostics = {
                globals = { "vim" },
            },
            workspace = {
                library = vim.api.nvim_get_runtime_file("", true),
                checkThirdParty = false,
            },
            telemetry = {
                enable = false,
            },
        },
    },
}
